<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/splash"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="16dp"
        android:layout_centerHorizontal="true"
        android:gravity="center_horizontal"
        android:padding="16dp">

        <Button
            android:id="@+id/btn_phone_sign_in"
            android:padding="16dp"
            android:layout_marginBottom="10dp"
            android:background="@color/buttonWhite"
            android:text="Iniciar sesion con tu telefono"
            android:fontFamily="@font/opensans_bold"
            android:drawableStart="@drawable/icphoneblue"
            android:textColor="@color/textButtonColor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/btn_google_sign_in"
            android:padding="16dp"
            android:background="@color/buttonWhite"
            android:text="Iniciar sesion con Google"
            android:fontFamily="@font/opensans_bold"
            android:drawableStart="@drawable/fui_ic_googleg_color_24dp"
            android:textColor="@color/textButtonColor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>

</RelativeLayout>